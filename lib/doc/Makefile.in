# Makefile.in generated by automake 1.16.2 from Makefile.am.
# @configure_input@

# Copyright (C) 1994-2020 Free Software Foundation, Inc.

# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

VPATH = @srcdir@
am__is_gnu_make = { \
  if test -z '$(MAKELEVEL)'; then \
    false; \
  elif test -n '$(MAKE_HOST)'; then \
    true; \
  elif test -n '$(MAKE_VERSION)' && test -n '$(CURDIR)'; then \
    true; \
  else \
    false; \
  fi; \
}
am__make_running_with_option = \
  case $${target_option-} in \
      ?) ;; \
      *) echo "am__make_running_with_option: internal error: invalid" \
              "target option '$${target_option-}' specified" >&2; \
         exit 1;; \
  esac; \
  has_opt=no; \
  sane_makeflags=$$MAKEFLAGS; \
  if $(am__is_gnu_make); then \
    sane_makeflags=$$MFLAGS; \
  else \
    case $$MAKEFLAGS in \
      *\\[\ \	]*) \
        bs=\\; \
        sane_makeflags=`printf '%s\n' "$$MAKEFLAGS" \
          | sed "s/$$bs$$bs[$$bs $$bs	]*//g"`;; \
    esac; \
  fi; \
  skip_next=no; \
  strip_trailopt () \
  { \
    flg=`printf '%s\n' "$$flg" | sed "s/$$1.*$$//"`; \
  }; \
  for flg in $$sane_makeflags; do \
    test $$skip_next = yes && { skip_next=no; continue; }; \
    case $$flg in \
      *=*|--*) continue;; \
        -*I) strip_trailopt 'I'; skip_next=yes;; \
      -*I?*) strip_trailopt 'I';; \
        -*O) strip_trailopt 'O'; skip_next=yes;; \
      -*O?*) strip_trailopt 'O';; \
        -*l) strip_trailopt 'l'; skip_next=yes;; \
      -*l?*) strip_trailopt 'l';; \
      -[dEDm]) skip_next=yes;; \
      -[JT]) skip_next=yes;; \
    esac; \
    case $$flg in \
      *$$target_option*) has_opt=yes; break;; \
    esac; \
  done; \
  test $$has_opt = yes
am__make_dryrun = (target_option=n; $(am__make_running_with_option))
am__make_keepgoing = (target_option=k; $(am__make_running_with_option))
pkgincludedir = $(includedir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkglibexecdir = $(libexecdir)/@PACKAGE@
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
target_triplet = @target@
subdir = lib/doc
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/config/lyxinclude.m4 \
	$(top_srcdir)/config/lyxpython.m4 $(top_srcdir)/config/pkg.m4 \
	$(top_srcdir)/config/qt4.m4 $(top_srcdir)/config/spell.m4 \
	$(top_srcdir)/m4/eilseq.m4 $(top_srcdir)/m4/iconv.m4 \
	$(top_srcdir)/m4/lib-ld.m4 $(top_srcdir)/m4/lib-link.m4 \
	$(top_srcdir)/m4/lib-prefix.m4 $(top_srcdir)/m4/nls.m4 \
	$(top_srcdir)/m4/po.m4 $(top_srcdir)/m4/progtest.m4 \
	$(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
DIST_COMMON = $(srcdir)/Makefile.am $(dist_ardoc_DATA) \
	$(dist_bibliodoc_DATA) $(dist_cadoc_DATA) \
	$(dist_clipartdoc_DATA) $(dist_csdoc_DATA) $(dist_dadoc_DATA) \
	$(dist_declipartdoc_DATA) $(dist_dedoc_DATA) $(dist_doc_DATA) \
	$(dist_eldoc_DATA) $(dist_esclipartdoc_DATA) \
	$(dist_esdoc_DATA) $(dist_eudoc_DATA) \
	$(dist_frclipartdoc_DATA) $(dist_frdoc_DATA) \
	$(dist_gldoc_DATA) $(dist_hedoc_DATA) $(dist_hudoc_DATA) \
	$(dist_idclipartdoc_DATA) $(dist_iddoc_DATA) \
	$(dist_itclipartdoc_DATA) $(dist_itdoc_DATA) \
	$(dist_jaclipartdoc_DATA) $(dist_jadoc_DATA) \
	$(dist_nbdoc_DATA) $(dist_nlclipartdoc_DATA) \
	$(dist_nldoc_DATA) $(dist_pldoc_DATA) $(dist_ptdoc_DATA) \
	$(dist_rodoc_DATA) $(dist_ruclipartdoc_DATA) \
	$(dist_rudoc_DATA) $(dist_skdoc_DATA) $(dist_sldoc_DATA) \
	$(dist_svdoc_DATA) $(dist_ukclipartdoc_DATA) \
	$(dist_ukdoc_DATA) $(dist_zhCNdoc_DATA) $(am__DIST_COMMON)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
AM_V_P = $(am__v_P_@AM_V@)
am__v_P_ = $(am__v_P_@AM_DEFAULT_V@)
am__v_P_0 = false
am__v_P_1 = :
AM_V_GEN = $(am__v_GEN_@AM_V@)
am__v_GEN_ = $(am__v_GEN_@AM_DEFAULT_V@)
am__v_GEN_0 = @echo "  GEN     " $@;
am__v_GEN_1 = 
AM_V_at = $(am__v_at_@AM_V@)
am__v_at_ = $(am__v_at_@AM_DEFAULT_V@)
am__v_at_0 = @
am__v_at_1 = 
SOURCES =
DIST_SOURCES =
am__can_run_installinfo = \
  case $$AM_UPDATE_INFO_DIR in \
    n|no|NO) false;; \
    *) (install-info --version) >/dev/null 2>&1;; \
  esac
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__uninstall_files_from_dir = { \
  test -z "$$files" \
    || { test ! -d "$$dir" && test ! -f "$$dir" && test ! -r "$$dir"; } \
    || { echo " ( cd '$$dir' && rm -f" $$files ")"; \
         $(am__cd) "$$dir" && rm -f $$files; }; \
  }
am__installdirs = "$(DESTDIR)$(ardocdir)" "$(DESTDIR)$(bibliodocdir)" \
	"$(DESTDIR)$(cadocdir)" "$(DESTDIR)$(clipartdocdir)" \
	"$(DESTDIR)$(csdocdir)" "$(DESTDIR)$(dadocdir)" \
	"$(DESTDIR)$(declipartdocdir)" "$(DESTDIR)$(dedocdir)" \
	"$(DESTDIR)$(docdir)" "$(DESTDIR)$(eldocdir)" \
	"$(DESTDIR)$(esclipartdocdir)" "$(DESTDIR)$(esdocdir)" \
	"$(DESTDIR)$(eudocdir)" "$(DESTDIR)$(frclipartdocdir)" \
	"$(DESTDIR)$(frdocdir)" "$(DESTDIR)$(gldocdir)" \
	"$(DESTDIR)$(hedocdir)" "$(DESTDIR)$(hudocdir)" \
	"$(DESTDIR)$(idclipartdocdir)" "$(DESTDIR)$(iddocdir)" \
	"$(DESTDIR)$(itclipartdocdir)" "$(DESTDIR)$(itdocdir)" \
	"$(DESTDIR)$(jaclipartdocdir)" "$(DESTDIR)$(jadocdir)" \
	"$(DESTDIR)$(nbdocdir)" "$(DESTDIR)$(nlclipartdocdir)" \
	"$(DESTDIR)$(nldocdir)" "$(DESTDIR)$(pldocdir)" \
	"$(DESTDIR)$(ptdocdir)" "$(DESTDIR)$(rodocdir)" \
	"$(DESTDIR)$(ruclipartdocdir)" "$(DESTDIR)$(rudocdir)" \
	"$(DESTDIR)$(skdocdir)" "$(DESTDIR)$(sldocdir)" \
	"$(DESTDIR)$(svdocdir)" "$(DESTDIR)$(ukclipartdocdir)" \
	"$(DESTDIR)$(ukdocdir)" "$(DESTDIR)$(zhCNdocdir)"
DATA = $(dist_ardoc_DATA) $(dist_bibliodoc_DATA) $(dist_cadoc_DATA) \
	$(dist_clipartdoc_DATA) $(dist_csdoc_DATA) $(dist_dadoc_DATA) \
	$(dist_declipartdoc_DATA) $(dist_dedoc_DATA) $(dist_doc_DATA) \
	$(dist_eldoc_DATA) $(dist_esclipartdoc_DATA) \
	$(dist_esdoc_DATA) $(dist_eudoc_DATA) \
	$(dist_frclipartdoc_DATA) $(dist_frdoc_DATA) \
	$(dist_gldoc_DATA) $(dist_hedoc_DATA) $(dist_hudoc_DATA) \
	$(dist_idclipartdoc_DATA) $(dist_iddoc_DATA) \
	$(dist_itclipartdoc_DATA) $(dist_itdoc_DATA) \
	$(dist_jaclipartdoc_DATA) $(dist_jadoc_DATA) \
	$(dist_nbdoc_DATA) $(dist_nlclipartdoc_DATA) \
	$(dist_nldoc_DATA) $(dist_pldoc_DATA) $(dist_ptdoc_DATA) \
	$(dist_rodoc_DATA) $(dist_ruclipartdoc_DATA) \
	$(dist_rudoc_DATA) $(dist_skdoc_DATA) $(dist_sldoc_DATA) \
	$(dist_svdoc_DATA) $(dist_ukclipartdoc_DATA) \
	$(dist_ukdoc_DATA) $(dist_zhCNdoc_DATA)
am__tagged_files = $(HEADERS) $(SOURCES) $(TAGS_FILES) $(LISP)
am__DIST_COMMON = $(srcdir)/Makefile.in $(top_srcdir)/config/common.am
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
pkgdatadir = @pkgdatadir@
ACLOCAL = @ACLOCAL@
AMTAR = @AMTAR@
AM_CPPFLAGS = @AM_CPPFLAGS@
AM_CXXFLAGS = @AM_CXXFLAGS@
AM_DEFAULT_VERBOSITY = @AM_DEFAULT_VERBOSITY@
AM_LDFLAGS = @AM_LDFLAGS@
AR = @AR@
ARFLAGS = @ARFLAGS@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
BOOST_INCLUDES = @BOOST_INCLUDES@
BOOST_LIBS = @BOOST_LIBS@
BROKEN_WCHAR_H = @BROKEN_WCHAR_H@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CPPFLAGS = @CPPFLAGS@
CXX = @CXX@
CXXCPP = @CXXCPP@
CXXDEPMODE = @CXXDEPMODE@
CXXFLAGS = @CXXFLAGS@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
DLL_VARIABLE = @DLL_VARIABLE@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
EILSEQ = @EILSEQ@
ENCHANT_CFLAGS = @ENCHANT_CFLAGS@
ENCHANT_LIBS = @ENCHANT_LIBS@
EXEEXT = @EXEEXT@
FRONTENDS_SUBDIRS = @FRONTENDS_SUBDIRS@
GETTEXT_MACRO_VERSION = @GETTEXT_MACRO_VERSION@
GMSGFMT = @GMSGFMT@
GMSGFMT_015 = @GMSGFMT_015@
GREP = @GREP@
HAVE_WCHAR_T = @HAVE_WCHAR_T@
HUNSPELL_CFLAGS = @HUNSPELL_CFLAGS@
HUNSPELL_LIBS = @HUNSPELL_LIBS@
ICONV_CONST = @ICONV_CONST@
ICONV_ICONV_H_IN = @ICONV_ICONV_H_IN@
ICONV_INCLUDES = @ICONV_INCLUDES@
ICONV_LIBS = @ICONV_LIBS@
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
LDFLAGS = @LDFLAGS@
LIBICONV = @LIBICONV@
LIBOBJS = @LIBOBJS@
LIBPSAPI = @LIBPSAPI@
LIBS = 
LIBSHLWAPI = @LIBSHLWAPI@
LTLIBICONV = @LTLIBICONV@
LTLIBOBJS = @LTLIBOBJS@
LYX_DATE = @LYX_DATE@
LYX_DIR_VER = @LYX_DIR_VER@
LYX_MAJOR_VERSION = @LYX_MAJOR_VERSION@
LYX_MINOR_VERSION = @LYX_MINOR_VERSION@
LYX_RELEASE_LEVEL = @LYX_RELEASE_LEVEL@
LYX_RELEASE_PATCH = @LYX_RELEASE_PATCH@
LYX_USERDIR_VER = @LYX_USERDIR_VER@
MAINT = @MAINT@
MAKEINFO = @MAKEINFO@
MKDIR_P = @MKDIR_P@
MSGFMT = @MSGFMT@
MSGFMT_015 = @MSGFMT_015@
MSGMERGE = @MSGMERGE@
MYTHES_INCLUDES = @MYTHES_INCLUDES@
MYTHES_LIBS = @MYTHES_LIBS@
OBJC = @OBJC@
OBJCDEPMODE = @OBJCDEPMODE@
OBJCFLAGS = @OBJCFLAGS@
OBJEXT = @OBJEXT@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_URL = @PACKAGE_URL@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
PKG_CONFIG = @PKG_CONFIG@
PYTHON = @PYTHON@
PYTHON_EXEC_PREFIX = @PYTHON_EXEC_PREFIX@
PYTHON_PLATFORM = @PYTHON_PLATFORM@
PYTHON_PREFIX = @PYTHON_PREFIX@
PYTHON_VERSION = @PYTHON_VERSION@
QTDIR = @QTDIR@
QTLIB_VERSION = @QTLIB_VERSION@
QT_CORE_CFLAGS = @QT_CORE_CFLAGS@
QT_CORE_INCLUDES = @QT_CORE_INCLUDES@
QT_CORE_LDFLAGS = @QT_CORE_LDFLAGS@
QT_CORE_LIB = @QT_CORE_LIB@
QT_CORE_LIBS = @QT_CORE_LIBS@
QT_CPPFLAGS = @QT_CPPFLAGS@
QT_FRONTEND_CFLAGS = @QT_FRONTEND_CFLAGS@
QT_FRONTEND_LIBS = @QT_FRONTEND_LIBS@
QT_INCLUDES = @QT_INCLUDES@
QT_LDFLAGS = @QT_LDFLAGS@
QT_LIB = @QT_LIB@
QT_MOC = @QT_MOC@
QT_RCC = @QT_RCC@
QT_UIC = @QT_UIC@
RANLIB = @RANLIB@
RC = @RC@
RPM_VERSION_SUFFIX = @RPM_VERSION_SUFFIX@
SED = @SED@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
SOCKET_LIBS = @SOCKET_LIBS@
STDLIB_DEBUG = @STDLIB_DEBUG@
STRIP = @STRIP@
USE_MBSTATE_T = @USE_MBSTATE_T@
USE_NLS = @USE_NLS@
USE_QT5 = @USE_QT5@
VERSION = @VERSION@
VERSION_INFO = @VERSION_INFO@
XGETTEXT = @XGETTEXT@
XGETTEXT_015 = @XGETTEXT_015@
XGETTEXT_EXTRA_OPTIONS = @XGETTEXT_EXTRA_OPTIONS@
XMKMF = @XMKMF@
X_CFLAGS = @X_CFLAGS@
X_EXTRA_LIBS = @X_EXTRA_LIBS@
X_LIBS = @X_LIBS@
X_PRE_LIBS = @X_PRE_LIBS@
ZLIB_INCLUDES = @ZLIB_INCLUDES@
ZLIB_LIBS = @ZLIB_LIBS@
abs_builddir = @abs_builddir@
abs_srcdir = @abs_srcdir@
abs_top_builddir = @abs_top_builddir@
abs_top_srcdir = @abs_top_srcdir@
ac_ct_AR = @ac_ct_AR@
ac_ct_CC = @ac_ct_CC@
ac_ct_CXX = @ac_ct_CXX@
ac_ct_OBJC = @ac_ct_OBJC@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
builddir = @builddir@
datadir = @datadir@
datarootdir = @datarootdir@
docdir = $(pkgdatadir)/doc
dvidir = @dvidir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
htmldir = @htmldir@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
lyx_devel_version = @lyx_devel_version@
mandir = @mandir@
mkdir_p = @mkdir_p@
moc_path = @moc_path@
oldincludedir = @oldincludedir@
osx_bundle_program_name = @osx_bundle_program_name@
pdfdir = @pdfdir@
pkgpyexecdir = @pkgpyexecdir@
pkgpythondir = @pkgpythondir@
prefix = @prefix@
program_base_name = @program_base_name@
program_suffix = @program_suffix@
program_transform_name = @program_transform_name@
psdir = @psdir@
pyexecdir = @pyexecdir@
pythondir = @pythondir@
qtc_path = @qtc_path@
rcc_path = @rcc_path@
real_pkgdatadir = @real_pkgdatadir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
srcdir = @srcdir@
sysconfdir = @sysconfdir@
target = @target@
target_alias = @target_alias@
target_cpu = @target_cpu@
target_os = @target_os@
target_vendor = @target_vendor@
top_build_prefix = @top_build_prefix@
top_builddir = @top_builddir@
top_srcdir = @top_srcdir@
uic_path = @uic_path@
version_suffix = @version_suffix@
DISTCLEANFILES = *.orig *.rej *~ *.bak core
MAINTAINERCLEANFILES = $(srcdir)/Makefile.in
AM_INSTALL_DATA_FLAGS = $(INSTALL) -m 644 -D
AM_ETAGSFLAGS = --lang=c++
dist_doc_DATA = \
	Additional.lyx \
	Customization.lyx \
	Development.lyx \
	DummyTextDocument.txt \
	DummyDocument1.lyx \
	DummyDocument2.lyx \
	EmbeddedObjects.lyx \
	Formula-numbering.lyx \
	Intro.lyx \
	LaTeXConfig.lyx \
	LFUNs.lyx \
	Math.lyx \
	Shortcuts.lyx \
	SpecialParagraphShape.tex \
	Tutorial.lyx \
	UserGuide.lyx

bibliodocdir = $(docdir)/biblio
dist_bibliodoc_DATA = \
	biblio/alphadin.bst \
	biblio/LyXDocs.bib

clipartdocdir = $(docdir)/clipart
dist_clipartdoc_DATA = \
	clipart/2D-intensity-plot.pdf \
	clipart/3D-structure-distort.pdf \
	clipart/Abstract.pdf \
	clipart/BoxInsetDefault.png \
	clipart/ChangesToolbar.png \
	clipart/ChildDocument.png \
	clipart/CommentNoteImage.png \
	clipart/endnotes.pdf \
	clipart/ERT.png \
	clipart/escher-lsd.eps \
	clipart/ExternalMaterial.png \
	clipart/ExtraToolbar.png \
	clipart/float.png \
	clipart/footnote.png \
	clipart/GreyedOutNoteImage.png \
	clipart/label.png \
	clipart/LyXNoteImage.png \
	clipart/macrobox.png \
	clipart/MacroToolbar.png \
	clipart/macrouse.png \
	clipart/mobius.eps \
	clipart/platypus.eps \
	clipart/reference.png \
	clipart/SpaceMarker.png \
	clipart/StandardToolbar.png \
	clipart/Star-structure.pdf \
	clipart/SVG-Drawing.svg \
	clipart/ToolbarEnvBox.png \
	clipart/ViewToolbar.png \
	clipart/without_fntright.pdf \
	clipart/with_fntright.pdf \
	clipart/workaround.png

ardocdir = $(docdir)/ar
dist_ardoc_DATA = \
	ar/Intro.lyx \
	ar/Shortcuts.lyx \
	ar/Tutorial.lyx

cadocdir = $(docdir)/ca
dist_cadoc_DATA = \
	ca/Intro.lyx

csdocdir = $(docdir)/cs
dist_csdoc_DATA = \
	cs/Tutorial.lyx

dadocdir = $(docdir)/da
dist_dadoc_DATA = \
	da/Intro.lyx

dedocdir = $(docdir)/de
dist_dedoc_DATA = \
	de/Additional.lyx \
	de/Customization.lyx \
	de/DummyDocument1.lyx \
	de/DummyDocument2.lyx \
	de/DummyTextDocument.txt \
	de/EmbeddedObjects.lyx \
	de/Formelnummerierung.lyx \
	de/Intro.lyx \
	de/Math.lyx \
	de/Shortcuts.lyx \
	de/Tutorial.lyx \
	de/UserGuide.lyx

declipartdocdir = $(dedocdir)/clipart
dist_declipartdoc_DATA = \
	de/clipart/ERT.png \
	de/clipart/ExternesMaterial.png \
	de/clipart/Fussnote.png \
	de/clipart/Gleitobjekt.png \
	de/clipart/GrauschriftNotiz.png \
	de/clipart/Kommentar.png \
	de/clipart/LyXNotiz.png \
	de/clipart/Marke.png \
	de/clipart/Querverweis.png \
	de/clipart/StandardBox.png \
	de/clipart/Zusammenfassung.pdf

eldocdir = $(docdir)/el
dist_eldoc_DATA = \
	el/Intro.lyx

esdocdir = $(docdir)/es
dist_esdoc_DATA = \
	es/Additional.lyx \
	es/Customization.lyx \
	es/DocumentoPostizo1.lyx \
	es/DocumentoPostizo2.lyx \
	es/DocumentoTextoPostizo.txt \
	es/EmbeddedObjects.lyx \
	es/Formula-numbering.lyx \
	es/Intro.lyx \
	es/Math.lyx \
	es/Shortcuts.lyx \
	es/Tutorial.lyx \
	es/UserGuide.lyx

esclipartdocdir = $(esdocdir)/clipart
dist_esclipartdoc_DATA = \
	es/clipart/ComentNotaImagen.png \
	es/clipart/CuadroMinipagina.png \
	es/clipart/DocumentoHijo.png \
	es/clipart/es_ERT.png \
	es/clipart/es_StandardToolbar.png \
	es/clipart/es_ToolbarEnvBox.png \
	es/clipart/etiqueta.png \
	es/clipart/flotante.png \
	es/clipart/GrisNotaImagen.png \
	es/clipart/MaterialExterno.png \
	es/clipart/NotaLyXImagen.png \
	es/clipart/notapie.png \
	es/clipart/referencia.png \
	es/clipart/Resumen.pdf

eudocdir = $(docdir)/eu
dist_eudoc_DATA = \
	eu/Intro.lyx \
	eu/Tutorial.lyx

frdocdir = $(docdir)/fr
dist_frdoc_DATA = \
	fr/Additional.lyx \
	fr/Customization.lyx \
	fr/DocumentBidon1.lyx \
	fr/DocumentBidon2.lyx \
	fr/DocumentTexteBidon.txt \
	fr/EmbeddedObjects.lyx \
	fr/Formula-numbering.lyx \
	fr/Intro.lyx \
	fr/Math.lyx \
	fr/Shortcuts.lyx \
	fr/Tutorial.lyx \
	fr/UserGuide.lyx

frclipartdocdir = $(frdocdir)/clipart
dist_frclipartdoc_DATA = \
	fr/clipart/BoxInsetDefault.png \
	fr/clipart/CommentNoteImage.png \
	fr/clipart/ERT.png \
	fr/clipart/float.png \
	fr/clipart/footnote.png \
	fr/clipart/GreyedOutNoteImage.png \
	fr/clipart/label.png \
	fr/clipart/LyXNoteImage.png \
	fr/clipart/reference.png \
	fr/clipart/SousDocument.png

gldocdir = $(docdir)/gl
dist_gldoc_DATA = \
	gl/Intro.lyx \
	gl/Tutorial.lyx

hedocdir = $(docdir)/he
dist_hedoc_DATA = \
	he/Intro.lyx \
	he/Tutorial.lyx

hudocdir = $(docdir)/hu
dist_hudoc_DATA = \
	hu/Intro.lyx \
	hu/Tutorial.lyx

iddocdir = $(docdir)/id
dist_iddoc_DATA = \
	id/Intro.lyx \
	id/Shortcuts.lyx \
	id/Tutorial.lyx

idclipartdocdir = $(iddocdir)/clipart
dist_idclipartdoc_DATA = \
	id/clipart/id-contrib.png \
	id/clipart/id-footnote.png \
	id/clipart/id-lingkungan.png

itdocdir = $(docdir)/it
dist_itdoc_DATA = \
	it/Intro.lyx \
	it/Tutorial.lyx

itclipartdocdir = $(itdocdir)/clipart
dist_itclipartdoc_DATA = \
	it/clipart/it_footnote.png \
	it/clipart/it_reference.png

jadocdir = $(docdir)/ja
dist_jadoc_DATA = \
	ja/Additional.lyx \
	ja/Customization.lyx \
	ja/DummyDocument1.lyx \
	ja/DummyDocument2.lyx \
	ja/DummyTextDocument.txt \
	ja/EmbeddedObjects.lyx \
	ja/Formula-numbering.lyx \
	ja/Intro.lyx \
	ja/LaTeXConfig.lyx \
	ja/Math.lyx \
	ja/Shortcuts.lyx \
	ja/Tutorial.lyx \
	ja/UserGuide.lyx

jaclipartdocdir = $(jadocdir)/clipart
dist_jaclipartdoc_DATA = \
	ja/clipart/BoxInsetDefault.png \
	ja/clipart/ChildDocument.png \
	ja/clipart/CommentNoteImage.png \
	ja/clipart/ERT.png \
	ja/clipart/ExternalMaterial.png \
	ja/clipart/float.png \
	ja/clipart/footnote.png \
	ja/clipart/GreyedOutNoteImage.png \
	ja/clipart/label.png \
	ja/clipart/LyXNoteImage.png \
	ja/clipart/macrobox.png \
	ja/clipart/reference.png \
	ja/clipart/StandardToolbar.png \
	ja/clipart/ToolbarEnvBox.png

nldocdir = $(docdir)/nl
dist_nldoc_DATA = \
	nl/Intro.lyx \
	nl/Shortcuts.lyx \
	nl/Tutorial.lyx

nlclipartdocdir = $(nldocdir)/clipart
dist_nlclipartdoc_DATA = \
	nl/clipart/verwijzing.png \
	nl/clipart/voetnoot.png

nbdocdir = $(docdir)/nb
dist_nbdoc_DATA = \
	nb/Intro.lyx \
	nb/Tutorial.lyx

pldocdir = $(docdir)/pl
dist_pldoc_DATA = \
	pl/Intro.lyx \
	pl/Tutorial.lyx

ptdocdir = $(docdir)/pt
dist_ptdoc_DATA = \
	pt/Intro.lyx \
	pt/Tutorial.lyx

rodocdir = $(docdir)/ro
dist_rodoc_DATA = \
	ro/Intro.lyx

rudocdir = $(docdir)/ru
dist_rudoc_DATA = \
	ru/Intro.lyx \
	ru/Tutorial.lyx

ruclipartdocdir = $(rudocdir)/clipart
dist_ruclipartdoc_DATA = \
	ru/clipart/ToolbarEnvBox.png

skdocdir = $(docdir)/sk
dist_skdoc_DATA = \
	sk/Intro.lyx \
	sk/Tutorial.lyx

sldocdir = $(docdir)/sl
dist_sldoc_DATA = \
	sl/Intro.lyx \
	sl/Tutorial.lyx

svdocdir = $(docdir)/sv
dist_svdoc_DATA = \
	sv/Intro.lyx \
	sv/Shortcuts.lyx \
	sv/Tutorial.lyx

ukdocdir = $(docdir)/uk
dist_ukdoc_DATA = \
	uk/Intro.lyx

ukclipartdocdir = $(ukdocdir)/clipart
dist_ukclipartdoc_DATA = \
	uk/clipart/footnote.png

zhCNdocdir = $(docdir)/zh_CN
dist_zhCNdoc_DATA = \
	zh_CN/Intro.lyx \
	zh_CN/Tutorial.lyx

doc_files = \
	    $(dist_doc_DATA) \
	    $(dist_cadoc_DATA) \
	    $(dist_csdoc_DATA) \
	    $(dist_dadoc_DATA) \
	    $(dist_dedoc_DATA) \
	    $(dist_esdoc_DATA) \
	    $(dist_eudoc_DATA) \
	    $(dist_frdoc_DATA) \
	    $(dist_gldoc_DATA) \
	    $(dist_hedoc_DATA) \
	    $(dist_hudoc_DATA) \
	    $(dist_itdoc_DATA) \
	    $(dist_jadoc_DATA) \
	    $(dist_nbdoc_DATA) \
	    $(dist_nldoc_DATA) \
	    $(dist_pldoc_DATA) \
	    $(dist_ptdoc_DATA) \
	    $(dist_rodoc_DATA) \
	    $(dist_rudoc_DATA) \
	    $(dist_skdoc_DATA) \
	    $(dist_sldoc_DATA) \
	    $(dist_svdoc_DATA) \
	    $(dist_ukdoc_DATA) \
	    $(dist_zhCNdoc_DATA)

all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in: @MAINTAINER_MODE_TRUE@ $(srcdir)/Makefile.am $(top_srcdir)/config/common.am $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign lib/doc/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --foreign lib/doc/Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__maybe_remake_depfiles)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__maybe_remake_depfiles);; \
	esac;
$(top_srcdir)/config/common.am $(am__empty):

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure: @MAINTAINER_MODE_TRUE@ $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4): @MAINTAINER_MODE_TRUE@ $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):
install-dist_ardocDATA: $(dist_ardoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_ardoc_DATA)'; test -n "$(ardocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(ardocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(ardocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ardocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ardocdir)" || exit $$?; \
	done

uninstall-dist_ardocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_ardoc_DATA)'; test -n "$(ardocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ardocdir)'; $(am__uninstall_files_from_dir)
install-dist_bibliodocDATA: $(dist_bibliodoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_bibliodoc_DATA)'; test -n "$(bibliodocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(bibliodocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(bibliodocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(bibliodocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(bibliodocdir)" || exit $$?; \
	done

uninstall-dist_bibliodocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_bibliodoc_DATA)'; test -n "$(bibliodocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(bibliodocdir)'; $(am__uninstall_files_from_dir)
install-dist_cadocDATA: $(dist_cadoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_cadoc_DATA)'; test -n "$(cadocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(cadocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(cadocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cadocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cadocdir)" || exit $$?; \
	done

uninstall-dist_cadocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_cadoc_DATA)'; test -n "$(cadocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(cadocdir)'; $(am__uninstall_files_from_dir)
install-dist_clipartdocDATA: $(dist_clipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_clipartdoc_DATA)'; test -n "$(clipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(clipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(clipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(clipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(clipartdocdir)" || exit $$?; \
	done

uninstall-dist_clipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_clipartdoc_DATA)'; test -n "$(clipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(clipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_csdocDATA: $(dist_csdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_csdoc_DATA)'; test -n "$(csdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(csdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(csdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(csdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(csdocdir)" || exit $$?; \
	done

uninstall-dist_csdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_csdoc_DATA)'; test -n "$(csdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(csdocdir)'; $(am__uninstall_files_from_dir)
install-dist_dadocDATA: $(dist_dadoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_dadoc_DATA)'; test -n "$(dadocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(dadocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(dadocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(dadocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(dadocdir)" || exit $$?; \
	done

uninstall-dist_dadocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_dadoc_DATA)'; test -n "$(dadocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(dadocdir)'; $(am__uninstall_files_from_dir)
install-dist_declipartdocDATA: $(dist_declipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_declipartdoc_DATA)'; test -n "$(declipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(declipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(declipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(declipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(declipartdocdir)" || exit $$?; \
	done

uninstall-dist_declipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_declipartdoc_DATA)'; test -n "$(declipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(declipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_dedocDATA: $(dist_dedoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_dedoc_DATA)'; test -n "$(dedocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(dedocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(dedocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(dedocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(dedocdir)" || exit $$?; \
	done

uninstall-dist_dedocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_dedoc_DATA)'; test -n "$(dedocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(dedocdir)'; $(am__uninstall_files_from_dir)
install-dist_docDATA: $(dist_doc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_doc_DATA)'; test -n "$(docdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(docdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(docdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(docdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(docdir)" || exit $$?; \
	done

uninstall-dist_docDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_doc_DATA)'; test -n "$(docdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(docdir)'; $(am__uninstall_files_from_dir)
install-dist_eldocDATA: $(dist_eldoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_eldoc_DATA)'; test -n "$(eldocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(eldocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(eldocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(eldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(eldocdir)" || exit $$?; \
	done

uninstall-dist_eldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_eldoc_DATA)'; test -n "$(eldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(eldocdir)'; $(am__uninstall_files_from_dir)
install-dist_esclipartdocDATA: $(dist_esclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_esclipartdoc_DATA)'; test -n "$(esclipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(esclipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(esclipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esclipartdocdir)" || exit $$?; \
	done

uninstall-dist_esclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_esclipartdoc_DATA)'; test -n "$(esclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(esclipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_esdocDATA: $(dist_esdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_esdoc_DATA)'; test -n "$(esdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(esdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(esdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esdocdir)" || exit $$?; \
	done

uninstall-dist_esdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_esdoc_DATA)'; test -n "$(esdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(esdocdir)'; $(am__uninstall_files_from_dir)
install-dist_eudocDATA: $(dist_eudoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_eudoc_DATA)'; test -n "$(eudocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(eudocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(eudocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(eudocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(eudocdir)" || exit $$?; \
	done

uninstall-dist_eudocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_eudoc_DATA)'; test -n "$(eudocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(eudocdir)'; $(am__uninstall_files_from_dir)
install-dist_frclipartdocDATA: $(dist_frclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_frclipartdoc_DATA)'; test -n "$(frclipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(frclipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(frclipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(frclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(frclipartdocdir)" || exit $$?; \
	done

uninstall-dist_frclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_frclipartdoc_DATA)'; test -n "$(frclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(frclipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_frdocDATA: $(dist_frdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_frdoc_DATA)'; test -n "$(frdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(frdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(frdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(frdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(frdocdir)" || exit $$?; \
	done

uninstall-dist_frdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_frdoc_DATA)'; test -n "$(frdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(frdocdir)'; $(am__uninstall_files_from_dir)
install-dist_gldocDATA: $(dist_gldoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_gldoc_DATA)'; test -n "$(gldocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gldocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gldocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gldocdir)" || exit $$?; \
	done

uninstall-dist_gldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_gldoc_DATA)'; test -n "$(gldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gldocdir)'; $(am__uninstall_files_from_dir)
install-dist_hedocDATA: $(dist_hedoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_hedoc_DATA)'; test -n "$(hedocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(hedocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(hedocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(hedocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(hedocdir)" || exit $$?; \
	done

uninstall-dist_hedocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_hedoc_DATA)'; test -n "$(hedocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(hedocdir)'; $(am__uninstall_files_from_dir)
install-dist_hudocDATA: $(dist_hudoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_hudoc_DATA)'; test -n "$(hudocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(hudocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(hudocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(hudocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(hudocdir)" || exit $$?; \
	done

uninstall-dist_hudocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_hudoc_DATA)'; test -n "$(hudocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(hudocdir)'; $(am__uninstall_files_from_dir)
install-dist_idclipartdocDATA: $(dist_idclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_idclipartdoc_DATA)'; test -n "$(idclipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(idclipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(idclipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(idclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(idclipartdocdir)" || exit $$?; \
	done

uninstall-dist_idclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_idclipartdoc_DATA)'; test -n "$(idclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(idclipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_iddocDATA: $(dist_iddoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_iddoc_DATA)'; test -n "$(iddocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(iddocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(iddocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(iddocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(iddocdir)" || exit $$?; \
	done

uninstall-dist_iddocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_iddoc_DATA)'; test -n "$(iddocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(iddocdir)'; $(am__uninstall_files_from_dir)
install-dist_itclipartdocDATA: $(dist_itclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_itclipartdoc_DATA)'; test -n "$(itclipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(itclipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(itclipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(itclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(itclipartdocdir)" || exit $$?; \
	done

uninstall-dist_itclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_itclipartdoc_DATA)'; test -n "$(itclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(itclipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_itdocDATA: $(dist_itdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_itdoc_DATA)'; test -n "$(itdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(itdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(itdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(itdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(itdocdir)" || exit $$?; \
	done

uninstall-dist_itdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_itdoc_DATA)'; test -n "$(itdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(itdocdir)'; $(am__uninstall_files_from_dir)
install-dist_jaclipartdocDATA: $(dist_jaclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_jaclipartdoc_DATA)'; test -n "$(jaclipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(jaclipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(jaclipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(jaclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(jaclipartdocdir)" || exit $$?; \
	done

uninstall-dist_jaclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_jaclipartdoc_DATA)'; test -n "$(jaclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(jaclipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_jadocDATA: $(dist_jadoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_jadoc_DATA)'; test -n "$(jadocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(jadocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(jadocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(jadocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(jadocdir)" || exit $$?; \
	done

uninstall-dist_jadocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_jadoc_DATA)'; test -n "$(jadocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(jadocdir)'; $(am__uninstall_files_from_dir)
install-dist_nbdocDATA: $(dist_nbdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_nbdoc_DATA)'; test -n "$(nbdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(nbdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(nbdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(nbdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(nbdocdir)" || exit $$?; \
	done

uninstall-dist_nbdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_nbdoc_DATA)'; test -n "$(nbdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(nbdocdir)'; $(am__uninstall_files_from_dir)
install-dist_nlclipartdocDATA: $(dist_nlclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_nlclipartdoc_DATA)'; test -n "$(nlclipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(nlclipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(nlclipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(nlclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(nlclipartdocdir)" || exit $$?; \
	done

uninstall-dist_nlclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_nlclipartdoc_DATA)'; test -n "$(nlclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(nlclipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_nldocDATA: $(dist_nldoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_nldoc_DATA)'; test -n "$(nldocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(nldocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(nldocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(nldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(nldocdir)" || exit $$?; \
	done

uninstall-dist_nldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_nldoc_DATA)'; test -n "$(nldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(nldocdir)'; $(am__uninstall_files_from_dir)
install-dist_pldocDATA: $(dist_pldoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_pldoc_DATA)'; test -n "$(pldocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(pldocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(pldocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(pldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(pldocdir)" || exit $$?; \
	done

uninstall-dist_pldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_pldoc_DATA)'; test -n "$(pldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(pldocdir)'; $(am__uninstall_files_from_dir)
install-dist_ptdocDATA: $(dist_ptdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_ptdoc_DATA)'; test -n "$(ptdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(ptdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(ptdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ptdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ptdocdir)" || exit $$?; \
	done

uninstall-dist_ptdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_ptdoc_DATA)'; test -n "$(ptdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ptdocdir)'; $(am__uninstall_files_from_dir)
install-dist_rodocDATA: $(dist_rodoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_rodoc_DATA)'; test -n "$(rodocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(rodocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(rodocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(rodocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(rodocdir)" || exit $$?; \
	done

uninstall-dist_rodocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_rodoc_DATA)'; test -n "$(rodocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(rodocdir)'; $(am__uninstall_files_from_dir)
install-dist_ruclipartdocDATA: $(dist_ruclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_ruclipartdoc_DATA)'; test -n "$(ruclipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(ruclipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(ruclipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ruclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ruclipartdocdir)" || exit $$?; \
	done

uninstall-dist_ruclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_ruclipartdoc_DATA)'; test -n "$(ruclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ruclipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_rudocDATA: $(dist_rudoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_rudoc_DATA)'; test -n "$(rudocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(rudocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(rudocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(rudocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(rudocdir)" || exit $$?; \
	done

uninstall-dist_rudocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_rudoc_DATA)'; test -n "$(rudocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(rudocdir)'; $(am__uninstall_files_from_dir)
install-dist_skdocDATA: $(dist_skdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_skdoc_DATA)'; test -n "$(skdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(skdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(skdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(skdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(skdocdir)" || exit $$?; \
	done

uninstall-dist_skdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_skdoc_DATA)'; test -n "$(skdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(skdocdir)'; $(am__uninstall_files_from_dir)
install-dist_sldocDATA: $(dist_sldoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_sldoc_DATA)'; test -n "$(sldocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(sldocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(sldocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(sldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(sldocdir)" || exit $$?; \
	done

uninstall-dist_sldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_sldoc_DATA)'; test -n "$(sldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(sldocdir)'; $(am__uninstall_files_from_dir)
install-dist_svdocDATA: $(dist_svdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_svdoc_DATA)'; test -n "$(svdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(svdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(svdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(svdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(svdocdir)" || exit $$?; \
	done

uninstall-dist_svdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_svdoc_DATA)'; test -n "$(svdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(svdocdir)'; $(am__uninstall_files_from_dir)
install-dist_ukclipartdocDATA: $(dist_ukclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_ukclipartdoc_DATA)'; test -n "$(ukclipartdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(ukclipartdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(ukclipartdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ukclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ukclipartdocdir)" || exit $$?; \
	done

uninstall-dist_ukclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_ukclipartdoc_DATA)'; test -n "$(ukclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ukclipartdocdir)'; $(am__uninstall_files_from_dir)
install-dist_ukdocDATA: $(dist_ukdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_ukdoc_DATA)'; test -n "$(ukdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(ukdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(ukdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ukdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ukdocdir)" || exit $$?; \
	done

uninstall-dist_ukdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_ukdoc_DATA)'; test -n "$(ukdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ukdocdir)'; $(am__uninstall_files_from_dir)
install-dist_zhCNdocDATA: $(dist_zhCNdoc_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_zhCNdoc_DATA)'; test -n "$(zhCNdocdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(zhCNdocdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(zhCNdocdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(zhCNdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(zhCNdocdir)" || exit $$?; \
	done

uninstall-dist_zhCNdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_zhCNdoc_DATA)'; test -n "$(zhCNdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(zhCNdocdir)'; $(am__uninstall_files_from_dir)
tags TAGS:

ctags CTAGS:

cscope cscopelist:


distdir: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) distdir-am

distdir-am: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(ardocdir)" "$(DESTDIR)$(bibliodocdir)" "$(DESTDIR)$(cadocdir)" "$(DESTDIR)$(clipartdocdir)" "$(DESTDIR)$(csdocdir)" "$(DESTDIR)$(dadocdir)" "$(DESTDIR)$(declipartdocdir)" "$(DESTDIR)$(dedocdir)" "$(DESTDIR)$(docdir)" "$(DESTDIR)$(eldocdir)" "$(DESTDIR)$(esclipartdocdir)" "$(DESTDIR)$(esdocdir)" "$(DESTDIR)$(eudocdir)" "$(DESTDIR)$(frclipartdocdir)" "$(DESTDIR)$(frdocdir)" "$(DESTDIR)$(gldocdir)" "$(DESTDIR)$(hedocdir)" "$(DESTDIR)$(hudocdir)" "$(DESTDIR)$(idclipartdocdir)" "$(DESTDIR)$(iddocdir)" "$(DESTDIR)$(itclipartdocdir)" "$(DESTDIR)$(itdocdir)" "$(DESTDIR)$(jaclipartdocdir)" "$(DESTDIR)$(jadocdir)" "$(DESTDIR)$(nbdocdir)" "$(DESTDIR)$(nlclipartdocdir)" "$(DESTDIR)$(nldocdir)" "$(DESTDIR)$(pldocdir)" "$(DESTDIR)$(ptdocdir)" "$(DESTDIR)$(rodocdir)" "$(DESTDIR)$(ruclipartdocdir)" "$(DESTDIR)$(rudocdir)" "$(DESTDIR)$(skdocdir)" "$(DESTDIR)$(sldocdir)" "$(DESTDIR)$(svdocdir)" "$(DESTDIR)$(ukclipartdocdir)" "$(DESTDIR)$(ukdocdir)" "$(DESTDIR)$(zhCNdocdir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	if test -z '$(STRIP)'; then \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	      install; \
	else \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	    "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'" install; \
	fi
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)
	-test -z "$(DISTCLEANFILES)" || rm -f $(DISTCLEANFILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
	-test -z "$(MAINTAINERCLEANFILES)" || rm -f $(MAINTAINERCLEANFILES)
clean: clean-am

clean-am: clean-generic mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-dist_ardocDATA install-dist_bibliodocDATA \
	install-dist_cadocDATA install-dist_clipartdocDATA \
	install-dist_csdocDATA install-dist_dadocDATA \
	install-dist_declipartdocDATA install-dist_dedocDATA \
	install-dist_docDATA install-dist_eldocDATA \
	install-dist_esclipartdocDATA install-dist_esdocDATA \
	install-dist_eudocDATA install-dist_frclipartdocDATA \
	install-dist_frdocDATA install-dist_gldocDATA \
	install-dist_hedocDATA install-dist_hudocDATA \
	install-dist_idclipartdocDATA install-dist_iddocDATA \
	install-dist_itclipartdocDATA install-dist_itdocDATA \
	install-dist_jaclipartdocDATA install-dist_jadocDATA \
	install-dist_nbdocDATA install-dist_nlclipartdocDATA \
	install-dist_nldocDATA install-dist_pldocDATA \
	install-dist_ptdocDATA install-dist_rodocDATA \
	install-dist_ruclipartdocDATA install-dist_rudocDATA \
	install-dist_skdocDATA install-dist_sldocDATA \
	install-dist_svdocDATA install-dist_ukclipartdocDATA \
	install-dist_ukdocDATA install-dist_zhCNdocDATA
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) install-data-hook
install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-dist_ardocDATA uninstall-dist_bibliodocDATA \
	uninstall-dist_cadocDATA uninstall-dist_clipartdocDATA \
	uninstall-dist_csdocDATA uninstall-dist_dadocDATA \
	uninstall-dist_declipartdocDATA uninstall-dist_dedocDATA \
	uninstall-dist_docDATA uninstall-dist_eldocDATA \
	uninstall-dist_esclipartdocDATA uninstall-dist_esdocDATA \
	uninstall-dist_eudocDATA uninstall-dist_frclipartdocDATA \
	uninstall-dist_frdocDATA uninstall-dist_gldocDATA \
	uninstall-dist_hedocDATA uninstall-dist_hudocDATA \
	uninstall-dist_idclipartdocDATA uninstall-dist_iddocDATA \
	uninstall-dist_itclipartdocDATA uninstall-dist_itdocDATA \
	uninstall-dist_jaclipartdocDATA uninstall-dist_jadocDATA \
	uninstall-dist_nbdocDATA uninstall-dist_nlclipartdocDATA \
	uninstall-dist_nldocDATA uninstall-dist_pldocDATA \
	uninstall-dist_ptdocDATA uninstall-dist_rodocDATA \
	uninstall-dist_ruclipartdocDATA uninstall-dist_rudocDATA \
	uninstall-dist_skdocDATA uninstall-dist_sldocDATA \
	uninstall-dist_svdocDATA uninstall-dist_ukclipartdocDATA \
	uninstall-dist_ukdocDATA uninstall-dist_zhCNdocDATA

.MAKE: install-am install-data-am install-strip

.PHONY: all all-am check check-am clean clean-generic cscopelist-am \
	ctags-am distclean distclean-generic distdir dvi dvi-am html \
	html-am info info-am install install-am install-data \
	install-data-am install-data-hook install-dist_ardocDATA \
	install-dist_bibliodocDATA install-dist_cadocDATA \
	install-dist_clipartdocDATA install-dist_csdocDATA \
	install-dist_dadocDATA install-dist_declipartdocDATA \
	install-dist_dedocDATA install-dist_docDATA \
	install-dist_eldocDATA install-dist_esclipartdocDATA \
	install-dist_esdocDATA install-dist_eudocDATA \
	install-dist_frclipartdocDATA install-dist_frdocDATA \
	install-dist_gldocDATA install-dist_hedocDATA \
	install-dist_hudocDATA install-dist_idclipartdocDATA \
	install-dist_iddocDATA install-dist_itclipartdocDATA \
	install-dist_itdocDATA install-dist_jaclipartdocDATA \
	install-dist_jadocDATA install-dist_nbdocDATA \
	install-dist_nlclipartdocDATA install-dist_nldocDATA \
	install-dist_pldocDATA install-dist_ptdocDATA \
	install-dist_rodocDATA install-dist_ruclipartdocDATA \
	install-dist_rudocDATA install-dist_skdocDATA \
	install-dist_sldocDATA install-dist_svdocDATA \
	install-dist_ukclipartdocDATA install-dist_ukdocDATA \
	install-dist_zhCNdocDATA install-dvi install-dvi-am \
	install-exec install-exec-am install-html install-html-am \
	install-info install-info-am install-man install-pdf \
	install-pdf-am install-ps install-ps-am install-strip \
	installcheck installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-generic pdf \
	pdf-am ps ps-am tags-am uninstall uninstall-am \
	uninstall-dist_ardocDATA uninstall-dist_bibliodocDATA \
	uninstall-dist_cadocDATA uninstall-dist_clipartdocDATA \
	uninstall-dist_csdocDATA uninstall-dist_dadocDATA \
	uninstall-dist_declipartdocDATA uninstall-dist_dedocDATA \
	uninstall-dist_docDATA uninstall-dist_eldocDATA \
	uninstall-dist_esclipartdocDATA uninstall-dist_esdocDATA \
	uninstall-dist_eudocDATA uninstall-dist_frclipartdocDATA \
	uninstall-dist_frdocDATA uninstall-dist_gldocDATA \
	uninstall-dist_hedocDATA uninstall-dist_hudocDATA \
	uninstall-dist_idclipartdocDATA uninstall-dist_iddocDATA \
	uninstall-dist_itclipartdocDATA uninstall-dist_itdocDATA \
	uninstall-dist_jaclipartdocDATA uninstall-dist_jadocDATA \
	uninstall-dist_nbdocDATA uninstall-dist_nlclipartdocDATA \
	uninstall-dist_nldocDATA uninstall-dist_pldocDATA \
	uninstall-dist_ptdocDATA uninstall-dist_rodocDATA \
	uninstall-dist_ruclipartdocDATA uninstall-dist_rudocDATA \
	uninstall-dist_skdocDATA uninstall-dist_sldocDATA \
	uninstall-dist_svdocDATA uninstall-dist_ukclipartdocDATA \
	uninstall-dist_ukdocDATA uninstall-dist_zhCNdocDATA

.PRECIOUS: Makefile


install-data-hook:
	for i in $(doc_files); \
	do \
		if $(EGREP) -q "LYX_DIR_VER|LYX_USERDIR_VER|^\\\\origin " "$(DESTDIR)$(docdir)/$$i"; then \
			origin=`dirname "/systemlyxdir/doc/$$i"`/; \
			sed -e "s/LYX_DIR_VER/$(LYX_DIR_VER)/g" \
			    -e "s/LYX_USERDIR_VER/$(LYX_USERDIR_VER)/g" \
			    -e "s|^\\\\origin .*|\\\\origin $$origin|" \
				"$(DESTDIR)$(docdir)/$$i" > "$(DESTDIR)$(docdir)/$$i.tmp"; \
			mv -f "$(DESTDIR)$(docdir)/$$i.tmp" "$(DESTDIR)$(docdir)/$$i"; \
			chmod 644 "$(DESTDIR)$(docdir)/$$i"; \
		fi; \
	done

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
